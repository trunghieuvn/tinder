import 'package:flutter/material.dart';

class MyColors {
  static const Color primaryColor = Colors.white;
  static const Color card_background =  const Color.fromRGBO(230, 230, 230, 1);
  static const Color card_border = Colors.grey;
}

class MySize {
  static const double CARD_BORDER_WIDTH = 1;
  static const double CARD_RADIUS = 200;
}